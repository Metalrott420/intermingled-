{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "node -e \"const fs = require('fs'); const p = fs.existsSync('./dist/index.mjs') ? './dist/index.mjs' : './artifacts/api-server/dist/index.mjs'; import(p);\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

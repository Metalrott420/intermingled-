[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.build]
cmds = [
  "pnpm install --no-frozen-lockfile",
  "pnpm --dir artifacts/speed-date build",
  "node artifacts/api-server/build.mjs"
]

[start]
cmd = "node artifacts/api-server/dist/index.mjs"
